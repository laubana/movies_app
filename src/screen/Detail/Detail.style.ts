import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    padding: 16,
    flex: 1,
    alignItems: "center",
  },
  title: {
    fontWeight: "bold",
    fontSize: 24,
    marginTop: 16,
    marginBottom: 32,
  },
  image: {
    width: "60%",
    aspectRatio: 1,
    marginBottom: 16,
  },
  body: {
    width: "80%",
    marginBottom: 16,
  },
});

export default styles;
